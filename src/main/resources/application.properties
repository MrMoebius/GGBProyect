spring.application.name=GGBProyect
spring.datasource.url=${DB_URL:jdbc:mysql://gateway01.eu-central-1.prod.aws.tidbcloud.com:4000/giber_bar?useSSL=true&serverTimezone=UTC}
spring.datasource.username=${DB_USERNAME:3afdqW4SQfTVGH7.root}
spring.datasource.password=${DB_PASSWORD:ijQ5zmngCKdPNTju}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# HikariCP Connection Pool
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.keepalive-time=300000

# JWT Configuration
app.jwt-secret=${JWT_SECRET:9a4f2c8d3b7a1e6f45c8a0b3f267d8b1d4e6f3c8a9d2b5f8e3a9c8b5f6e4d1a2}
app.jwt-expiration-milliseconds=${JWT_EXPIRATION:604800000}
logging.level.org.springframework.security=WARN

# CORS
app.cors.allowed-origins=${CORS_ORIGINS:http://localhost:4200,http://localhost:5173}

# Pagination
spring.data.web.pageable.default-page-size=20
spring.data.web.pageable.max-page-size=10000

# File uploads
app.upload.games-dir=${UPLOAD_GAMES_DIR:uploads/games}
app.upload.events-dir=${UPLOAD_EVENTS_DIR:uploads/events}
app.upload.clientes-dir=${UPLOAD_CLIENTES_DIR:uploads/clientes}
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# Actuator (solo health para Docker health checks)
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=never

# Esto es pa la config de email (Gmail SMTP) lode verificacion de cuentas. NO TOQUEIS PUTAs
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=davizssj107@gmail.com
spring.mail.password=tsjajtjhskpboiat
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# forma parte de lo mismo, pero para la verificacion de email: horas de expiracion del token y URL base para el enlace
app.email-verification.expiration-hours=${EMAIL_VERIFICATION_EXPIRATION_HOURS:24}
# La URL base apunta al frontend de Angular (puerto 4200) para que el enlace del email
# lleve al usuario al formulario de contraseña, no directamente al backend
app.email-verification.base-url=${EMAIL_VERIFICATION_BASE_URL:http://localhost:4200}

# BoardGameGeek XML API
bgg.api-token=${BGG_API_TOKEN:e3f8c3ff-9926-4efc-863c-3b92acda4d32}
